rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY: Allow all authenticated users full access
    // This is used for emulator seeding and testing
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
